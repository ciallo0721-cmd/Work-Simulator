# 2026-06-14 工作日志

## 打工模拟器 bug修复 + 旧版Windows适配

### 修复内容
- **文件**: `G:\EmoScan Pro\Work-Simulator\源码\打工模拟器.cpp`
- **Bug**: 食物商店（`shop_type == 2`）选 `0. 返回` 无法退出
- **原因**: `case 0: break;` 只跳出了 `switch`，没跳出外层的 `while (true)` 循环
- **修复**: 在 `switch` 结束后、`if (choice > 0)` 之前加了 `if (choice == 0) break;`（第4775行）

### 旧版Windows适配
- **新文件**: `G:\EmoScan Pro\Work-Simulator\源码\打工模拟器(适配旧版本Windows).cpp`
- **改动**:
  1. 所有 `COLOR_*` 宏清空为 `""`（禁用ANSI颜色转义序列）
  2. `CLEAR_SCREEN()` 宏在 Windows 下改为 `system("cls")`
  3. `enable_ansi()` 函数体清空（空函数）
  4. 文件头注释更新为"旧版本Windows适配版"
- **目标**: 兼容 Windows XP/Vista/7 等不支持ANSI转义序列的旧版本
