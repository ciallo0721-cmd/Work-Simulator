# 2026-04-18

## 打工模拟器项目修复

### 源码修复 (打工模拟器.cpp)
- 修复第256行 `MAX_ACHIEVENTS` 拼写错误 → `MAX_ACHIEVEMENTS`，连锁修复20+个 `has no member 'achievements'` 编译错误
- 修复第2532行和第3298行 `36500 * 86400` 整数溢出 warning，加 `(time_t)` 强制转换

### 修改器重写 (修改器/修改器.py v2.0)
- 通过 Python 模拟 C 结构体对齐，精确计算 Player 结构体所有字段偏移量
- Player 结构体大小: 37648 bytes，存档文件: 37652 bytes (含4字节版本头)
- 修复 `kun_coins`/`total_income`/`total_expenses` 等字段类型：旧代码用 `int`(4字节)，实际为 `money_t`(8字节 `long long`)
- 添加缺失字段: `prestige_points`, `boss_defeats`, `festival_participations`, `craft_count`
- 存档版本检测 (SAVE_VERSION=3)，自动备份功能
- 修正所有偏移量（旧偏移全部错误，例如 furniture_count 旧值28→正确值2532）

### README 更新 (README.MD)
- 版本号 v2.0 → v3.0，同步存档格式信息（37652字节）
- 修改器 v2.0 说明，Python 3.6+ 要求
- 更新游戏内容描述（30成就、15任务、合成系统、好友系统等）

### 环境备注
- Python 路径: `C:\Users\nb666\AppData\Local\Programs\Python\Python313\python.exe`
- 系统 `python` 命令不可用（PATH 指向 Microsoft Store stub），需用绝对路径
- gcc 不可用，无法在本地编译 C 代码验证偏移量
